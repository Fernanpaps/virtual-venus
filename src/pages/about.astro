---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = 'Acerca de mí';

const identity = {
	firstName: 'Fernando',
	country: 'México',
	occupation: 'estudiante de Desarrollo de Software',
	hobbies: ['programar', 'videojuegos', 'música', 'futbol'],
};

const skills = ['HTML', 'CSS', 'JavaScript', 'React', 'Astro'];

const happy = true;
const finished = true;
const goal = 3;

const skillColor = 'navy';
const fontWeight = 'bold';
const textCase = 'uppercase';
---

<BaseLayout pageTitle={pageTitle}>
	<h2>... y mi nuevo sitio con Astro</h2>

	<p>Estoy trabajando en el tutorial de Astro. Esta es la segunda página que agrego a mi sitio.</p>

	<p>Aquí algunos datos sobre mí:</p>
	<ul>
		<li>Mi nombre es {identity.firstName}.</li>
		<li>Vivo en {identity.country} y soy {identity.occupation}.</li>
		{identity.hobbies.length >= 2 && (
			<li>Dos de mis pasatiempos son: {identity.hobbies[0]} y {identity.hobbies[1]}</li>
		)}
	</ul>

	<p>Mis habilidades son:</p>
	<ul>
		{skills.map((skill) => <li class="skill">{skill}</li>)}
	</ul>

	{happy && <p>¡Estoy feliz de estar aprendiendo Astro!</p>}

	{finished && <p>Terminé este tutorial.</p>}

	{goal === 3 ? <p>Mi meta es terminar en 3 días.</p> : <p>Mi meta no es de 3 días.</p>}
</BaseLayout>

<style define:vars={{ skillColor, fontWeight, textCase }}>
	.skill {
		color: var(--skillColor);
		font-weight: var(--fontWeight);
		text-transform: var(--textCase);
	}
</style>
